diff --git a/src/components/widgets/6-cards-items/CardsItems.astro b/src/components/widgets/6-cards-items/CardsItems.astro
index 7c06542..f1c0589 100644
--- a/src/components/widgets/6-cards-items/CardsItems.astro
+++ b/src/components/widgets/6-cards-items/CardsItems.astro
@@ -151,6 +151,7 @@ const svgArrow = `<svg class="svg-arrow" width="24" height="24" viewBox="0 0 24
         <div class="flex flex-wrap gap-8 justify-center mx-auto">
           {cards.map((card) => (
             <div class:list={[
+              'hk-card-6-3',
               baseClasses.cardBase,
               baseClasses.cardBaseMd,
               tokens.card,
@@ -376,7 +377,7 @@ const svgArrow = `<svg class="svg-arrow" width="24" height="24" viewBox="0 0 24
         </div>
         <div class="flex flex-wrap gap-8 justify-center mx-auto">
           {cards.map((card) => (
-            <div class={baseClasses.cardBaseV610}>
+            <div class:list={['hk-card-6-10', baseClasses.cardBaseV610]}>
               <div class="overflow-hidden relative rounded-t-[2rem]">
                 <img src={card.image ?? placeholderImgLarge(card.title || 'Card')} class="w-full transition-transform duration-500 hk-card-imagen lg:group-hover:scale-110" alt="Imagen del card" />
               </div>
@@ -437,6 +438,7 @@ const svgArrow = `<svg class="svg-arrow" width="24" height="24" viewBox="0 0 24
         <div class="flex flex-wrap gap-8 justify-center mx-auto">
           {cards.map((card) => (
             <div class:list={[
+              'hk-card-6-11',
               baseClasses.cardBaseV611,
               baseClasses.cardBaseV611Lg,
               cardWidthV611(cardCount),
@@ -509,6 +511,7 @@ const svgArrow = `<svg class="svg-arrow" width="24" height="24" viewBox="0 0 24
         <div class="flex flex-wrap gap-8 justify-center mx-auto">
           {cards.map((card) => (
             <div class:list={[
+              'hk-card-6-12',
               baseClasses.cardBaseV612,
               baseClasses.cardBaseV612Lg,
               cardWidthV612(cardCount),
diff --git a/src/pages/index.astro b/src/pages/index.astro
index 25a9991..3e49a42 100644
--- a/src/pages/index.astro
+++ b/src/pages/index.astro
@@ -947,6 +947,32 @@ const busList = Object.entries(buColors.bus).map(([id, palette]) => ({
     // Si querés cambiar un icono, editá solo ese archivo.
     const ICONS_SVG = ICON_PATHS;
 
+    // ── CardsItems (6) — templates de card por variante × modo ─
+    // Selector explícito por variante (cada card wrapper tiene su propia
+    // clase `hk-card-6-N`, ver CardsItems.astro).
+    const CARD6_SELECTOR =
+      '.hk-card-6-3, .hk-card-6-6, .hk-card-6-8, .hk-card-6-10, .hk-card-6-11, .hk-card-6-12';
+
+    // Guarda un clon "pristino" del primer card renderizado por el servidor,
+    // por cada combinación variant×mode, para poder clonar cards nuevas aunque
+    // el usuario haya borrado todas las originales del DOM.
+    const card6Templates = new Map();
+
+    function captureCard6Templates() {
+      document
+        .querySelectorAll('[data-preview-instance="6-cards-items"]')
+        .forEach((inst) => {
+          const variant = inst.dataset.previewVariant;
+          const mode = inst.dataset.previewMode;
+          const variantContent = inst.querySelector(`[data-variant-content="${variant}"]`);
+          const firstCard = variantContent?.querySelector(CARD6_SELECTOR);
+          if (firstCard) {
+            card6Templates.set(`${variant}|${mode}`, firstCard.cloneNode(true));
+          }
+        });
+    }
+    captureCard6Templates();
+
     function getCards(widgetId) {
       // Lee el estado actual del editor de cards desde el DOM.
       const editor = document.querySelector(`[data-cards-editor="${widgetId}"]`);
@@ -1001,15 +1027,27 @@ const busList = Object.entries(buColors.bus).map(([id, palette]) => ({
         // ── CardsItems (6) — busca cards DENTRO del data-variant-content activo ──
         // Cada instance tiene varios [data-variant-content=v6.X] (uno por variante).
         // Solo la activa matchea `inst.dataset.previewVariant`.
-        const variantContent = inst.querySelector(
-          `[data-variant-content="${inst.dataset.previewVariant}"]`
-        );
+        const variant = inst.dataset.previewVariant;
+        const mode = inst.dataset.previewMode;
+        const variantContent = inst.querySelector(`[data-variant-content="${variant}"]`);
         if (!variantContent) return;
 
-        // Selector unificado por variante (incluye variantes sin prefijo "hk-card-6-").
-        const cardEls = variantContent.querySelectorAll(
-          '.hk-card-6-3, .hk-card-6-6, .hk-card-6-8, .hk-card'
-        );
+        let cardEls = Array.from(variantContent.querySelectorAll(CARD6_SELECTOR));
+        const container = cardEls[0]?.parentElement;
+        const template = card6Templates.get(`${variant}|${mode}`);
+
+        // Sincronizar cantidad de nodos DOM con la cantidad de cards del editor:
+        // clonar el template por cada card nueva, eliminar los sobrantes.
+        if (container && template) {
+          while (cardEls.length < cards.length) {
+            const clone = template.cloneNode(true);
+            container.appendChild(clone);
+            cardEls.push(clone);
+          }
+          while (cardEls.length > cards.length) {
+            cardEls.pop()?.remove();
+          }
+        }
 
         cardEls.forEach((cardEl, i) => {
           const c = cards[i];
