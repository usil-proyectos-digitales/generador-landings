diff --git a/src/components/widgets/6-cards-items/CardsItems.astro b/src/components/widgets/6-cards-items/CardsItems.astro
index f1c0589..7b4d3c8 100644
--- a/src/components/widgets/6-cards-items/CardsItems.astro
+++ b/src/components/widgets/6-cards-items/CardsItems.astro
@@ -147,6 +147,14 @@ const svgArrow = `<svg class="svg-arrow" width="24" height="24" viewBox="0 0 24
               </div>
             )}
           </h2>
+          {description && (
+            <p class:list={[
+              'hk-desc w-full md:w-[90%] text-pretty',
+              baseClasses.bodyFont,
+              tokens.desc,
+              'text-mont-block-p-mobile md:text-mont-block-p',
+            ]}>{description}</p>
+          )}
         </div>
         <div class="flex flex-wrap gap-8 justify-center mx-auto">
           {cards.map((card) => (
@@ -158,11 +166,22 @@ const svgArrow = `<svg class="svg-arrow" width="24" height="24" viewBox="0 0 24
               cardWidthV63(cardCount),
             ]}>
               <div class="flex gap-4 justify-center items-center">
-                <img src={card.icon ?? `${WIDGET_ASSETS}/card_imagen.webp`} class="hk-card-imagen w-[75px]" alt="Imagen del Card" />
+                {/* Imagen opcional (ACF: icono 75×75px). Sin imagen, el
+                    título ocupa todo el ancho — ver clase w-full abajo.
+                    Se renderiza siempre con `hidden` (en vez de omitirse)
+                    para que el live-edit del Visor solo tenga que alternar
+                    visibilidad + src, sin crear/destruir el nodo. */}
+                <img
+                  src={card.icon ?? ''}
+                  hidden={!card.icon}
+                  class="hk-card-imagen w-[75px] h-[75px] object-contain shrink-0"
+                  alt="Imagen del Card"
+                />
                 <h3 class:list={[
                   'hk-card-title',
                   baseClasses.cardTitleFont,
                   tokens.cardTitle,
+                  !card.icon && 'w-full',
                 ]}>{card.title}</h3>
               </div>
               <p class:list={[
@@ -434,6 +453,14 @@ const svgArrow = `<svg class="svg-arrow" width="24" height="24" viewBox="0 0 24
               </div>
             )}
           </h2>
+          {description && (
+            <p class:list={[
+              'hk-desc w-full md:w-[90%] text-pretty',
+              baseClasses.bodyFont,
+              tokens.desc,
+              'text-mont-block-p-mobile md:text-mont-block-p',
+            ]}>{description}</p>
+          )}
         </div>
         <div class="flex flex-wrap gap-8 justify-center mx-auto">
           {cards.map((card) => (
@@ -507,6 +534,14 @@ const svgArrow = `<svg class="svg-arrow" width="24" height="24" viewBox="0 0 24
               </div>
             )}
           </h2>
+          {description && (
+            <p class:list={[
+              'hk-desc w-full md:w-[90%] text-pretty',
+              baseClasses.bodyFont,
+              tokens.desc,
+              'text-mont-block-p-mobile md:text-mont-block-p',
+            ]}>{description}</p>
+          )}
         </div>
         <div class="flex flex-wrap gap-8 justify-center mx-auto">
           {cards.map((card) => (
diff --git a/src/components/widgets/6-cards-items/theme.ts b/src/components/widgets/6-cards-items/theme.ts
index 21f7ad1..241579b 100644
--- a/src/components/widgets/6-cards-items/theme.ts
+++ b/src/components/widgets/6-cards-items/theme.ts
@@ -207,7 +207,7 @@ export const theme: Record<Variant, Record<Mode, VariantTheme>> = {
       title: 'text-bu-primary',
       titleSupportBox: 'bg-bu-primary',
       titleSupport: 'text-bu-surface',
-      desc: '',
+      desc: 'text-bu-primary',
       card: 'bg-bu-primary',
       cardIconBox: '',
       cardTitle: 'text-bu-surface',
@@ -222,7 +222,7 @@ export const theme: Record<Variant, Record<Mode, VariantTheme>> = {
       title: 'text-bu-neutral',
       titleSupportBox: 'bg-bu-neutral',
       titleSupport: 'text-bu-primary',
-      desc: '',
+      desc: 'text-bu-neutral',
       card: 'bg-bu-neutral',
       cardIconBox: '',
       cardTitle: 'text-bu-primary',
@@ -240,7 +240,7 @@ export const theme: Record<Variant, Record<Mode, VariantTheme>> = {
       title: 'text-bu-primary',
       titleSupportBox: 'bg-bu-primary',
       titleSupport: 'text-bu-surface',
-      desc: '',
+      desc: 'text-bu-primary',
       card: 'bg-bu-primary',
       cardIconBox: '',
       cardTitle: 'text-bu-surface',
@@ -255,7 +255,7 @@ export const theme: Record<Variant, Record<Mode, VariantTheme>> = {
       title: 'text-bu-neutral',
       titleSupportBox: 'bg-bu-neutral',
       titleSupport: 'text-bu-primary',
-      desc: '',
+      desc: 'text-bu-neutral',
       card: 'bg-bu-neutral',
       cardIconBox: '',
       cardTitle: 'text-bu-primary',
diff --git a/src/components/widgets/registry.ts b/src/components/widgets/registry.ts
index 15b4fd1..ed4436b 100644
--- a/src/components/widgets/registry.ts
+++ b/src/components/widgets/registry.ts
@@ -23,6 +23,14 @@ import SaveTheDate from './5-save-the-date/SaveTheDate.astro';
 import CardsItems from './6-cards-items/CardsItems.astro';
 import type { Mode } from './2-bloque-intro/theme.ts';
 
+/** Un campo editable por card en el editor del drawer (widgets "cards6"). */
+export interface Card6FieldSpec {
+  /** Clave del campo en el objeto card (title, text, icon, ctaText, ctaHref, ...). */
+  key: string;
+  /** Label visible en el editor del drawer. */
+  label: string;
+}
+
 /** Definición de una variante visual de un widget. */
 export interface WidgetVariant {
   id: string;
@@ -35,8 +43,22 @@ export interface WidgetVariant {
    */
   // eslint-disable-next-line @typescript-eslint/no-explicit-any
   cards?: any[];
+  /**
+   * Schema de campos editables por card para ESTA variante (solo widgets
+   * "cards6", ver `data-cards-kind` en el Visor). Cada variante puede tener
+   * una estructura de card muy distinta — si se omite, el editor usa el
+   * default de 3 campos (`DEFAULT_CARD6_FIELDS`: título/texto/CTA texto).
+   */
+  cardFields?: Card6FieldSpec[];
 }
 
+/** Schema default del editor de cards cuando una variante no declara el suyo propio. */
+export const DEFAULT_CARD6_FIELDS: Card6FieldSpec[] = [
+  { key: 'title', label: 'Título' },
+  { key: 'text', label: 'Texto' },
+  { key: 'ctaText', label: 'CTA' },
+];
+
 /** Props canónicas que el visor sabe manejar (live editing). */
 export interface WidgetDefaults {
   title: string;
@@ -136,7 +158,7 @@ function makeCards6(count: number) {
     title: titles[i % titles.length],
     text: 'Consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.',
     ctaText: 'Inscríbete',
-    ctaHref: '#',
+    ctaHref: '',
   }));
 }
 
@@ -197,7 +219,22 @@ export const widgetRegistry: WidgetRegistration[] = [
     variants: [
       // Cantidad inicial de cards acordada por variante — cada una arranca
       // independiente de las demás (no comparten props ni conteo).
-      { id: 'v6.3', label: '6.3 — Cards Centradas', cards: makeCards6(3) },
+      {
+        id: 'v6.3',
+        label: '6.3 — Cards Centradas',
+        cards: makeCards6(3),
+        // v6.3 permite reemplazar el ícono default por una imagen propia
+        // (75×75px, opcional — sin imagen el título ocupa todo el ancho) y
+        // un link de CTA propio. Ninguna otra variante necesita estos
+        // campos hoy, por eso el schema es específico de v6.3.
+        cardFields: [
+          { key: 'title', label: 'Título' },
+          { key: 'text', label: 'Texto' },
+          { key: 'icon', label: 'Imagen (URL, 75×75px — opcional)' },
+          { key: 'ctaText', label: 'CTA' },
+          { key: 'ctaHref', label: 'CTA URL' },
+        ],
+      },
       { id: 'v6.6', label: '6.6 — Cards Icono Izq', cards: makeCards6(6) },
       { id: 'v6.8', label: '6.8 — Cards Verticales', cards: makeCards6(5) },
       { id: 'v6.10', label: '6.10 — Dos Cards Grandes', cards: makeCards6(2) },
diff --git a/src/data/widgets-catalog.json b/src/data/widgets-catalog.json
index 6f9b92a..236d221 100644
--- a/src/data/widgets-catalog.json
+++ b/src/data/widgets-catalog.json
@@ -34,7 +34,7 @@
     {
       "id": "cards",
       "name": "Cards estáticas",
-      "variants": ["v6-6", "v6-8", "v6-10", "v6-11", "v6-12"],
+      "variants": ["v6-3", "v6-6", "v6-8", "v6-10", "v6-11", "v6-12"],
       "description": "Grid de cards con título, contenido y opcional CTA",
       "props": {
         "section_title": { "type": "string" },
diff --git a/src/pages/index.astro b/src/pages/index.astro
index da5d9a8..ebfe026 100644
--- a/src/pages/index.astro
+++ b/src/pages/index.astro
@@ -33,7 +33,12 @@
  *   Ver `generador-landings/src/components/widgets/registry.ts` (registry central).
  */
 import BaseLayout from '@layouts/BaseLayout.astro';
-import { widgetRegistry, type WidgetRegistration } from '../components/widgets/registry.ts';
+import {
+  widgetRegistry,
+  DEFAULT_CARD6_FIELDS,
+  type WidgetRegistration,
+  type Card6FieldSpec,
+} from '../components/widgets/registry.ts';
 import { ICON_PATHS } from '../components/widgets/5-save-the-date/icons.ts';
 import buColors from '@data/bu-colors.json';
 
@@ -55,6 +60,20 @@ function defaultCardsFor(w: WidgetRegistration) {
 function cardsByVariant(w: WidgetRegistration) {
   return Object.fromEntries(w.variants.map((v) => [v.id, v.cards ?? w.defaults.cards ?? []]));
 }
+
+// Schema de campos editables por card de una variante — cada variante puede
+// tener una estructura distinta (ver `WidgetVariant.cardFields`); si no
+// declara uno propio, usa el default de 3 campos (título/texto/CTA).
+function cardFieldsFor(w: WidgetRegistration, variantId: string): Card6FieldSpec[] {
+  return w.variants.find((v) => v.id === variantId)?.cardFields ?? DEFAULT_CARD6_FIELDS;
+}
+
+// Mapa variantId → schema de campos, para que el editor del cliente sepa
+// qué inputs mostrar al cambiar de variante (sin acceso a registry.ts, que
+// es server-only).
+function cardFieldsByVariant(w: WidgetRegistration) {
+  return Object.fromEntries(w.variants.map((v) => [v.id, v.cardFields ?? DEFAULT_CARD6_FIELDS]));
+}
 ---
 
 <BaseLayout
@@ -416,8 +435,11 @@ function cardsByVariant(w: WidgetRegistration) {
       gap: 0.5rem;
     }
     .visor__card-row {
+      /* Cantidad de campos variable según el schema de la variante (ver
+         `cardFieldsFor` en el frontmatter) — auto-fit acomoda tanto la fila
+         de 3 campos default como la de 5 campos de v6.3, sin reglas por kind. */
       display: grid;
-      grid-template-columns: auto 1fr 1fr auto;
+      grid-template-columns: repeat(auto-fit, minmax(6.5rem, 1fr)) auto;
       gap: 0.375rem;
       align-items: center;
     }
@@ -651,6 +673,14 @@ function cardsByVariant(w: WidgetRegistration) {
             <script is:inline type="application/json" data-cards-defaults={w.id} set:html={JSON.stringify(cardsByVariant(w))} />
           )}
 
+          {/* Schema de campos por variante (título/texto/CTA por default,
+              o el custom que declare la variante — ver v6.3). Le dice al
+              editor del cliente qué inputs reconstruir al cambiar de
+              variante. */}
+          {w.variants.some((v) => v.cards) && (
+            <script is:inline type="application/json" data-cards-fields={w.id} set:html={JSON.stringify(cardFieldsByVariant(w))} />
+          )}
+
           <div class="visor__widget-body" data-widget-body={w.id}>
             {/* Preview — todas las variantes pre-renderizadas; JS muestra/oculta */}
             <div class="visor__preview-wrap">
@@ -803,29 +833,21 @@ function cardsByVariant(w: WidgetRegistration) {
                   >
                     {defaultCardsFor(w)!.map((card: any, i: number) => {
                       if (w.id === '6-cards-items') {
+                        // Campos dirigidos por schema — cada variante puede
+                        // declarar los suyos (ver `cardFieldsFor`); v6.3 por
+                        // ejemplo agrega Imagen y CTA URL además del default.
+                        const fields = cardFieldsFor(w, w.defaultVariant);
                         return (
                           <div class="visor__card-row" data-card-row={i}>
-                            <input
-                              type="text"
-                              placeholder="Título"
-                              value={card.title ?? ''}
-                              data-card-field={`title-${w.id}-${i}`}
-                              aria-label="Título"
-                            />
-                            <input
-                              type="text"
-                              placeholder="Texto"
-                              value={card.text ?? ''}
-                              data-card-field={`text-${w.id}-${i}`}
-                              aria-label="Texto"
-                            />
-                            <input
-                              type="text"
-                              placeholder="CTA"
-                              value={card.ctaText ?? ''}
-                              data-card-field={`ctaText-${w.id}-${i}`}
-                              aria-label="CTA"
-                            />
+                            {fields.map((field) => (
+                              <input
+                                type="text"
+                                placeholder={field.label}
+                                value={card[field.key] ?? ''}
+                                data-field-key={field.key}
+                                aria-label={field.label}
+                              />
+                            ))}
                             <button
                               type="button"
                               class="visor__card-remove"
@@ -1076,42 +1098,69 @@ function cardsByVariant(w: WidgetRegistration) {
       return card6StateByVariant.get(key);
     }
 
-    // Lee las filas del editor (DOM) tal como están ahora mismo.
+    // ── Schema de campos por variante ───────────────────────────
+    // Cada variante puede declarar su propia lista de campos editables por
+    // card (ver `Card6FieldSpec`/`cardFields` en registry.ts — v6.3 agrega
+    // Imagen y CTA URL). Sin schema propio, cae al default de 3 campos.
+    const DEFAULT_FIELDS = [
+      { key: 'title', label: 'Título' },
+      { key: 'text', label: 'Texto' },
+      { key: 'ctaText', label: 'CTA' },
+    ];
+    const card6FieldsCache = new Map(); // key: widgetId -> {variant: fieldSpec[]}
+
+    function readCard6Fields(widgetId) {
+      if (card6FieldsCache.has(widgetId)) return card6FieldsCache.get(widgetId);
+      const script = document.querySelector(`script[data-cards-fields="${widgetId}"]`);
+      let parsed = {};
+      if (script) {
+        try {
+          parsed = JSON.parse(script.textContent || '{}');
+        } catch {
+          parsed = {};
+        }
+      }
+      card6FieldsCache.set(widgetId, parsed);
+      return parsed;
+    }
+
+    function getCard6Fields(widgetId, variant) {
+      return readCard6Fields(widgetId)[variant] ?? DEFAULT_FIELDS;
+    }
+
+    // Lee las filas del editor (DOM) tal como están ahora mismo — genérico
+    // sobre cualquier schema de campos (matchea por [data-field-key], no
+    // por título del label, así funciona igual para 3 o 5+ campos).
     function snapshotCard6Rows(editor) {
       const rows = editor.querySelectorAll('[data-card-row]');
-      return Array.from(rows).map((row) => ({
-        title: row.querySelector('input[aria-label="Título"]')?.value ?? '',
-        text: row.querySelector('input[aria-label="Texto"]')?.value ?? '',
-        ctaText: row.querySelector('input[aria-label="CTA"]')?.value ?? '',
-      }));
+      return Array.from(rows).map((row) => {
+        const card = {};
+        row.querySelectorAll('[data-field-key]').forEach((input) => {
+          card[input.dataset.fieldKey] = input.value ?? '';
+        });
+        return card;
+      });
     }
 
-    // Reconstruye las filas del editor a partir de un array de cards
-    // (usado al cambiar de variante, para cargar el estado de la nueva).
-    function renderCard6Rows(editor, cards) {
+    // Reconstruye las filas del editor a partir de un array de cards y el
+    // schema de campos de la variante activa (usado al cambiar de variante
+    // o al agregar una card nueva).
+    function renderCard6Rows(editor, cards, fields) {
       editor.innerHTML = '';
       cards.forEach((c, i) => {
         const row = document.createElement('div');
         row.className = 'visor__card-row';
         row.setAttribute('data-card-row', String(i));
 
-        const titleInput = document.createElement('input');
-        titleInput.type = 'text';
-        titleInput.placeholder = 'Título';
-        titleInput.setAttribute('aria-label', 'Título');
-        titleInput.value = c.title ?? '';
-
-        const textInput = document.createElement('input');
-        textInput.type = 'text';
-        textInput.placeholder = 'Texto';
-        textInput.setAttribute('aria-label', 'Texto');
-        textInput.value = c.text ?? '';
-
-        const ctaInput = document.createElement('input');
-        ctaInput.type = 'text';
-        ctaInput.placeholder = 'CTA';
-        ctaInput.setAttribute('aria-label', 'CTA');
-        ctaInput.value = c.ctaText ?? '';
+        fields.forEach((field) => {
+          const input = document.createElement('input');
+          input.type = 'text';
+          input.placeholder = field.label;
+          input.setAttribute('aria-label', field.label);
+          input.setAttribute('data-field-key', field.key);
+          input.value = c[field.key] ?? '';
+          row.appendChild(input);
+        });
 
         const removeBtn = document.createElement('button');
         removeBtn.type = 'button';
@@ -1119,8 +1168,8 @@ function cardsByVariant(w: WidgetRegistration) {
         removeBtn.setAttribute('data-card-remove', String(i));
         removeBtn.setAttribute('aria-label', 'Eliminar card');
         removeBtn.textContent = '✕';
+        row.appendChild(removeBtn);
 
-        row.append(titleInput, textInput, ctaInput, removeBtn);
         editor.appendChild(row);
       });
     }
@@ -1234,6 +1283,20 @@ function cardsByVariant(w: WidgetRegistration) {
             // Si no hay CTA, ocultar el <a>
             if (!c.ctaText) ctaEl.style.display = 'none';
             else ctaEl.style.display = '';
+            // Link del CTA (interno o externo) — solo lo tocan las
+            // variantes cuyo schema declara `ctaHref` (hoy, v6.3).
+            if (c.ctaHref !== undefined) ctaEl.setAttribute('href', c.ctaHref || '#');
+          }
+          // Imagen opcional del card (solo v6.3 hoy — ver `cardFields` en
+          // registry.ts). Sin valor, se oculta y el título ocupa el ancho
+          // completo (sin caer a ningún placeholder).
+          if (c.icon !== undefined) {
+            const imgEl = cardEl.querySelector('.hk-card-imagen');
+            if (imgEl) {
+              imgEl.hidden = !c.icon;
+              if (c.icon) imgEl.src = c.icon;
+            }
+            if (titleEl) titleEl.classList.toggle('w-full', !c.icon);
           }
         });
 
@@ -1280,7 +1343,10 @@ function cardsByVariant(w: WidgetRegistration) {
                 snapshotCard6Rows(cardsEditor)
               );
               const nextCards = getOrSeedCard6State(widgetId, newVariant);
-              renderCard6Rows(cardsEditor, nextCards);
+              // El schema de campos (cantidad/orden de inputs por card)
+              // también es propio de cada variante — se reconstruye la
+              // fila entera, no solo los valores.
+              renderCard6Rows(cardsEditor, nextCards, getCard6Fields(widgetId, newVariant));
             }
 
             instances.forEach((inst) => {
@@ -1303,7 +1369,10 @@ function cardsByVariant(w: WidgetRegistration) {
               const h = doc.querySelector('.hk-title');
               if (h) h.textContent = ctrl.value;
             } else if (control === 'description') {
-              const d = doc.querySelector('.hk-desc');
+              // v6.8/v6.10 usan clases propias para el mismo párrafo de
+              // Bajada en vez de `hk-desc` — cubrir las 3 para que el
+              // live-edit funcione en todas las variantes que la usan.
+              const d = doc.querySelector('.hk-desc, .hk-paragraph-6-8, .hk-text-paragraph');
               if (d) d.innerHTML = ctrl.value;
             } else if (control === 'titleSupport') {
               const s = doc.querySelector('.hk-title-soporte');
@@ -1358,12 +1427,28 @@ function cardsByVariant(w: WidgetRegistration) {
         row.className = 'visor__card-row';
         row.setAttribute('data-card-row', String(nextIdx));
         if (template === 'cards6') {
-          row.innerHTML = `
-            <input type="text" placeholder="Título" aria-label="Título" />
-            <input type="text" placeholder="Texto" aria-label="Texto" />
-            <input type="text" placeholder="CTA" aria-label="CTA" />
-            <button type="button" class="visor__card-remove" data-card-remove="${nextIdx}" aria-label="Eliminar card">✕</button>
-          `;
+          // Campos según el schema de la variante ACTIVA (no un template
+          // fijo) — así una card nueva en v6.3 nace con sus 5 campos, y en
+          // el resto de las variantes con los 3 default.
+          const activeVariant = document
+            .querySelector(`[data-preview-instance="${widgetId}"]:not([hidden])`)
+            ?.dataset.previewVariant;
+          const fields = getCard6Fields(widgetId, activeVariant);
+          fields.forEach((field) => {
+            const input = document.createElement('input');
+            input.type = 'text';
+            input.placeholder = field.label;
+            input.setAttribute('aria-label', field.label);
+            input.setAttribute('data-field-key', field.key);
+            row.appendChild(input);
+          });
+          const removeBtn = document.createElement('button');
+          removeBtn.type = 'button';
+          removeBtn.className = 'visor__card-remove';
+          removeBtn.setAttribute('data-card-remove', String(nextIdx));
+          removeBtn.setAttribute('aria-label', 'Eliminar card');
+          removeBtn.textContent = '✕';
+          row.appendChild(removeBtn);
         } else {
           row.innerHTML = `
             <select class="visor__card-icon" aria-label="Icono">
