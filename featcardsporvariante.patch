diff --git a/src/components/widgets/registry.ts b/src/components/widgets/registry.ts
index aa0ab0a..f6770a2 100644
--- a/src/components/widgets/registry.ts
+++ b/src/components/widgets/registry.ts
@@ -27,6 +27,14 @@ import type { Mode } from './2-bloque-intro/theme.ts';
 export interface WidgetVariant {
   id: string;
   label: string;
+  /**
+   * Cards por defecto específicas de esta variante (si el widget usa `cards`).
+   * Cuando se define, tiene prioridad sobre `defaults.cards` — así cada
+   * variante arranca con su propia cantidad/contenido, en vez de compartir
+   * un único set entre todas las variantes del widget.
+   */
+  // eslint-disable-next-line @typescript-eslint/no-explicit-any
+  cards?: any[];
 }
 
 /** Props canónicas que el visor sabe manejar (live editing). */
@@ -71,6 +79,29 @@ export interface WidgetRegistration {
   propsAdapter?: PropsAdapter;
 }
 
+/**
+ * Genera N cards de relleno (Lorem Ipsum) para sembrar el default de una
+ * variante de `6-cards-items`. El contenido es irrelevante — lo único que
+ * importa acá es la CANTIDAD, que varía por variante (ver comentario en
+ * cada entrada de `variants` más abajo).
+ */
+function makeCards6(count: number) {
+  const titles = [
+    'Lorem ipsum dolor sit',
+    'Sed do eiusmod tempor',
+    'Dolor sit amet consectetur',
+    'Ut enim ad minim veniam',
+    'Excepteur sint occaecat',
+    'Duis aute irure dolor',
+  ];
+  return Array.from({ length: count }, (_, i) => ({
+    title: titles[i % titles.length],
+    text: 'Consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.',
+    ctaText: 'Inscríbete',
+    ctaHref: '#',
+  }));
+}
+
 /** Catálogo de widgets. Ampliar aquí cuando se agreguen nuevos widgets. */
 export const widgetRegistry: WidgetRegistration[] = [
   {
@@ -126,12 +157,14 @@ export const widgetRegistry: WidgetRegistration[] = [
     description:
       'Widget de cards versátil con 6 variantes: cards centradas (v6.3), icono izq (v6.6), verticales con imagen (v6.8), dos cards grandes (v6.10), cards venta+CTA (v6.11) y cards con fecha (v6.12). Soporta modo claro y oscuro.',
     variants: [
-      { id: 'v6.3', label: '6.3 — Cards Centradas' },
-      { id: 'v6.6', label: '6.6 — Cards Icono Izq' },
-      { id: 'v6.8', label: '6.8 — Cards Verticales' },
-      { id: 'v6.10', label: '6.10 — Dos Cards Grandes' },
-      { id: 'v6.11', label: '6.11 — Cards Venta + CTA' },
-      { id: 'v6.12', label: '6.12 — Cards con Fecha' },
+      // Cantidad inicial de cards acordada por variante — cada una arranca
+      // independiente de las demás (no comparten props ni conteo).
+      { id: 'v6.3', label: '6.3 — Cards Centradas', cards: makeCards6(3) },
+      { id: 'v6.6', label: '6.6 — Cards Icono Izq', cards: makeCards6(6) },
+      { id: 'v6.8', label: '6.8 — Cards Verticales', cards: makeCards6(5) },
+      { id: 'v6.10', label: '6.10 — Dos Cards Grandes', cards: makeCards6(2) },
+      { id: 'v6.11', label: '6.11 — Cards Venta + CTA', cards: makeCards6(3) },
+      { id: 'v6.12', label: '6.12 — Cards con Fecha', cards: makeCards6(3) },
     ],
     defaultVariant: 'v6.3',
     defaults: {
@@ -139,26 +172,9 @@ export const widgetRegistry: WidgetRegistration[] = [
       titleSupport: 'resaltar beneficios',
       description: 'Descripción introductoria de la sección.',
       mode: 'light',
-      cards: [
-        {
-          title: 'Lorem ipsum dolor sit',
-          text: 'Consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.',
-          ctaText: 'Inscríbete',
-          ctaHref: '#',
-        },
-        {
-          title: 'Sed do eiusmod tempor',
-          text: 'Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip.',
-          ctaText: 'Inscríbete',
-          ctaHref: '#',
-        },
-        {
-          title: 'Dolor sit amet consectetur',
-          text: 'Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia.',
-          ctaText: 'Inscríbete',
-          ctaHref: '#',
-        },
-      ],
+      // Fallback genérico (variantes sin `cards` propio caerían acá).
+      // La cantidad real por variante vive en `variants[].cards` arriba.
+      cards: makeCards6(3),
     },
     component: CardsItems,
     propsAdapter: ({ title, description, variant, id, mode, cards, titleSupport }) => ({
diff --git a/src/pages/index.astro b/src/pages/index.astro
index 3e49a42..d027f3c 100644
--- a/src/pages/index.astro
+++ b/src/pages/index.astro
@@ -33,7 +33,7 @@
  *   Ver `generador-landings/src/components/widgets/registry.ts` (registry central).
  */
 import BaseLayout from '@layouts/BaseLayout.astro';
-import { widgetRegistry } from '../components/widgets/registry.ts';
+import { widgetRegistry, type WidgetRegistration } from '../components/widgets/registry.ts';
 import { ICON_PATHS } from '../components/widgets/5-save-the-date/icons.ts';
 import buColors from '@data/bu-colors.json';
 
@@ -42,6 +42,19 @@ const busList = Object.entries(buColors.bus).map(([id, palette]) => ({
   id,
   ...(palette as { primary: string; secondary: string; accent: string; surface: string; neutral: string }),
 }));
+
+// Cards de la variante default de un widget — usa el override por-variante
+// si existe, si no cae al `defaults.cards` compartido (caso de widgets con
+// una sola variante, como 5-save-the-date).
+function defaultCardsFor(w: WidgetRegistration) {
+  return w.variants.find((v) => v.id === w.defaultVariant)?.cards ?? w.defaults.cards;
+}
+
+// Mapa variantId → cards, para hidratar el estado del editor en el cliente
+// cuando el usuario cambia de variante por primera vez.
+function cardsByVariant(w: WidgetRegistration) {
+  return Object.fromEntries(w.variants.map((v) => [v.id, v.cards ?? w.defaults.cards ?? []]));
+}
 ---
 
 <BaseLayout
@@ -621,6 +634,15 @@ const busList = Object.entries(buColors.bus).map(([id, palette]) => ({
             </div>
           </header>
 
+          {/* Defaults de cards por variante (si el widget las usa), para que
+              el editor del cliente pueda sembrar el estado de una variante
+              la primera vez que se visita — sin esto no habría forma de
+              conocer, en el navegador, cuántas/cuáles cards le tocan a cada
+              variante que todavía no se renderizó en el editor. */}
+          {w.variants.some((v) => v.cards) && (
+            <script is:inline type="application/json" data-cards-defaults={w.id} set:html={JSON.stringify(cardsByVariant(w))} />
+          )}
+
           <div class="visor__widget-body" data-widget-body={w.id}>
             {/* Preview — todas las variantes pre-renderizadas; JS muestra/oculta */}
             <div class="visor__preview-wrap">
@@ -631,6 +653,11 @@ const busList = Object.entries(buColors.bus).map(([id, palette]) => ({
                       // ID único por combinación variant × mode (sin espacios problemáticos).
                       const previewId = `preview-${w.id}-${v.id}-${mode}`;
                       
+                      // Cada variante usa sus propias cards por defecto si las
+                      // define (`v.cards`); si no, cae al set compartido del
+                      // widget (`w.defaults.cards`, caso de una sola variante).
+                      const variantCards = v.cards ?? w.defaults.cards;
+
                       const adapterInput = {
                         title: w.defaults.title,
                         description: w.defaults.description,
@@ -638,7 +665,7 @@ const busList = Object.entries(buColors.bus).map(([id, palette]) => ({
                         id: previewId,
                         mode: mode,
                         ...(w.defaults.titleSupport ? { titleSupport: w.defaults.titleSupport } : {}),
-                        ...(w.defaults.cards ? { cards: w.defaults.cards } : {}),
+                        ...(variantCards ? { cards: variantCards } : {}),
                       };
 
                       const componentProps = w.propsAdapter
@@ -761,8 +788,10 @@ const busList = Object.entries(buColors.bus).map(([id, palette]) => ({
                 </div>
               )}
 
-              {/* Editor de cards (solo si el widget las expone en defaults) */}
-              {w.defaults.cards && (
+              {/* Editor de cards (solo si el widget las expone). Arranca con
+                  las cards de la variante DEFAULT — cada variante tiene su
+                  propia cantidad/contenido, no se comparte entre variantes. */}
+              {defaultCardsFor(w) && (
                 <div class="visor__control">
                   <label>
                     Cards
@@ -777,7 +806,7 @@ const busList = Object.entries(buColors.bus).map(([id, palette]) => ({
                     data-cards-editor={w.id}
                     data-cards-kind={w.id === '6-cards-items' ? 'cards6' : 'std'}
                   >
-                    {w.defaults.cards.map((card: any, i: number) => {
+                    {defaultCardsFor(w)!.map((card: any, i: number) => {
                       if (w.id === '6-cards-items') {
                         return (
                           <div class="visor__card-row" data-card-row={i}>
@@ -973,20 +1002,97 @@ const busList = Object.entries(buColors.bus).map(([id, palette]) => ({
     }
     captureCard6Templates();
 
+    // ── Estado de cards "cards6" por widget × variante ──────────
+    // Cada variante tiene su propia cantidad/contenido de cards, totalmente
+    // independiente de las demás (no se comparten props entre variantes).
+    // El editor del drawer SIEMPRE refleja la variante activa; cuando el
+    // usuario cambia de variante, el estado de la anterior se guarda acá y
+    // el de la nueva se restaura (o se siembra desde los defaults del
+    // servidor si es la primera vez que se visita esa variante).
+    const card6StateByVariant = new Map(); // key: `${widgetId}|${variant}` -> cards[]
+    const card6DefaultsCache = new Map(); // key: widgetId -> {variant: cards[]}
+
+    function readCard6Defaults(widgetId) {
+      if (card6DefaultsCache.has(widgetId)) return card6DefaultsCache.get(widgetId);
+      const script = document.querySelector(`script[data-cards-defaults="${widgetId}"]`);
+      let parsed = {};
+      if (script) {
+        try {
+          parsed = JSON.parse(script.textContent || '{}');
+        } catch {
+          parsed = {};
+        }
+      }
+      card6DefaultsCache.set(widgetId, parsed);
+      return parsed;
+    }
+
+    function getOrSeedCard6State(widgetId, variant) {
+      const key = `${widgetId}|${variant}`;
+      if (!card6StateByVariant.has(key)) {
+        const seed = readCard6Defaults(widgetId)[variant] ?? [];
+        card6StateByVariant.set(key, seed.map((c) => ({ ...c })));
+      }
+      return card6StateByVariant.get(key);
+    }
+
+    // Lee las filas del editor (DOM) tal como están ahora mismo.
+    function snapshotCard6Rows(editor) {
+      const rows = editor.querySelectorAll('[data-card-row]');
+      return Array.from(rows).map((row) => ({
+        title: row.querySelector('input[aria-label="Título"]')?.value ?? '',
+        text: row.querySelector('input[aria-label="Texto"]')?.value ?? '',
+        ctaText: row.querySelector('input[aria-label="CTA"]')?.value ?? '',
+      }));
+    }
+
+    // Reconstruye las filas del editor a partir de un array de cards
+    // (usado al cambiar de variante, para cargar el estado de la nueva).
+    function renderCard6Rows(editor, cards) {
+      editor.innerHTML = '';
+      cards.forEach((c, i) => {
+        const row = document.createElement('div');
+        row.className = 'visor__card-row';
+        row.setAttribute('data-card-row', String(i));
+
+        const titleInput = document.createElement('input');
+        titleInput.type = 'text';
+        titleInput.placeholder = 'Título';
+        titleInput.setAttribute('aria-label', 'Título');
+        titleInput.value = c.title ?? '';
+
+        const textInput = document.createElement('input');
+        textInput.type = 'text';
+        textInput.placeholder = 'Texto';
+        textInput.setAttribute('aria-label', 'Texto');
+        textInput.value = c.text ?? '';
+
+        const ctaInput = document.createElement('input');
+        ctaInput.type = 'text';
+        ctaInput.placeholder = 'CTA';
+        ctaInput.setAttribute('aria-label', 'CTA');
+        ctaInput.value = c.ctaText ?? '';
+
+        const removeBtn = document.createElement('button');
+        removeBtn.type = 'button';
+        removeBtn.className = 'visor__card-remove';
+        removeBtn.setAttribute('data-card-remove', String(i));
+        removeBtn.setAttribute('aria-label', 'Eliminar card');
+        removeBtn.textContent = '✕';
+
+        row.append(titleInput, textInput, ctaInput, removeBtn);
+        editor.appendChild(row);
+      });
+    }
+
     function getCards(widgetId) {
       // Lee el estado actual del editor de cards desde el DOM.
       const editor = document.querySelector(`[data-cards-editor="${widgetId}"]`);
       if (!editor) return [];
       const kind = editor.dataset.cardsKind ?? 'std';
+      if (kind === 'cards6') return snapshotCard6Rows(editor);
       const rows = editor.querySelectorAll('[data-card-row]');
       return Array.from(rows).map((row) => {
-        if (kind === 'cards6') {
-          return {
-            title: row.querySelector('input[aria-label="Título"]')?.value ?? '',
-            text: row.querySelector('input[aria-label="Texto"]')?.value ?? '',
-            ctaText: row.querySelector('input[aria-label="CTA"]')?.value ?? '',
-          };
-        }
         const icon = row.querySelector('.visor__card-icon')?.value ?? 'fecha';
         const label = row.querySelector('input[aria-label="Etiqueta"]')?.value ?? '';
         const value = row.querySelector('input[aria-label="Valor"]')?.value ?? '';
@@ -995,17 +1101,30 @@ const busList = Object.entries(buColors.bus).map(([id, palette]) => ({
     }
 
     function renderCards(widgetId) {
-      const cards = getCards(widgetId);
       const editor = document.querySelector(`[data-cards-editor="${widgetId}"]`);
       const kind = editor?.dataset.cardsKind ?? 'std';
 
+      // 'cards6': el editor siempre refleja la variante actualmente visible.
+      // Antes de renderizar nada, guardamos ese estado en vivo para esa
+      // variante puntual (las demás variantes mantienen el suyo, sin tocarse).
+      if (kind === 'cards6' && editor) {
+        const activeVariant = document
+          .querySelector(`[data-preview-instance="${widgetId}"]:not([hidden])`)
+          ?.dataset.previewVariant;
+        if (activeVariant) {
+          card6StateByVariant.set(`${widgetId}|${activeVariant}`, snapshotCard6Rows(editor));
+        }
+      }
+
+      const stdCards = kind === 'std' ? getCards(widgetId) : null;
+
       const instances = document.querySelectorAll(`[data-preview-instance="${widgetId}"]`);
       instances.forEach((inst) => {
         // ── Save the Date ─────────────────────────────────
         if (kind === 'std') {
           const container = inst.querySelector('.hk-std-cards-container');
           if (!container) return;
-          const valid = cards.filter((c) => c.label?.trim() && c.value?.trim()).slice(0, 6);
+          const valid = stdCards.filter((c) => c.label?.trim() && c.value?.trim()).slice(0, 6);
           container.innerHTML = valid
             .map((c) => {
               const path = ICONS_SVG[c.iconSlug] ?? ICONS_SVG.fecha;
@@ -1029,6 +1148,8 @@ const busList = Object.entries(buColors.bus).map(([id, palette]) => ({
         // Solo la activa matchea `inst.dataset.previewVariant`.
         const variant = inst.dataset.previewVariant;
         const mode = inst.dataset.previewMode;
+        // Cards propias de ESTA variante — independientes de las demás.
+        const cards = getOrSeedCard6State(widgetId, variant);
         const variantContent = inst.querySelector(`[data-variant-content="${variant}"]`);
         if (!variantContent) return;
 
@@ -1036,8 +1157,8 @@ const busList = Object.entries(buColors.bus).map(([id, palette]) => ({
         const container = cardEls[0]?.parentElement;
         const template = card6Templates.get(`${variant}|${mode}`);
 
-        // Sincronizar cantidad de nodos DOM con la cantidad de cards del editor:
-        // clonar el template por cada card nueva, eliminar los sobrantes.
+        // Sincronizar cantidad de nodos DOM con la cantidad de cards de ESTA
+        // variante: clonar el template por cada card nueva, eliminar sobrantes.
         if (container && template) {
           while (cardEls.length < cards.length) {
             const clone = template.cloneNode(true);
@@ -1089,18 +1210,37 @@ const busList = Object.entries(buColors.bus).map(([id, palette]) => ({
           if (!instances.length) return;
 
           if (control === 'variant') {
-            const v = ctrl.value;
+            const newVariant = ctrl.value;
+            const visibleInst = document.querySelector(
+              `[data-preview-instance="${widgetId}"]:not([hidden])`
+            );
+            const oldVariant = visibleInst?.dataset.previewVariant;
             // Detectar el modo actual (de la instancia visible).
-            const currentMode =
-              document
-                .querySelector(`[data-preview-instance="${widgetId}"]:not([hidden])`)
-                ?.dataset.previewMode ?? 'light';
+            const currentMode = visibleInst?.dataset.previewMode ?? 'light';
+
+            // Cards (widgets tipo 'cards6'): cada variante tiene su propio
+            // estado independiente. Al cambiar, guardamos el de la variante
+            // que se abandona y cargamos el de la nueva en el editor.
+            const cardsEditor = document.querySelector(
+              `[data-cards-editor="${widgetId}"][data-cards-kind="cards6"]`
+            );
+            if (cardsEditor && oldVariant && oldVariant !== newVariant) {
+              card6StateByVariant.set(
+                `${widgetId}|${oldVariant}`,
+                snapshotCard6Rows(cardsEditor)
+              );
+              const nextCards = getOrSeedCard6State(widgetId, newVariant);
+              renderCard6Rows(cardsEditor, nextCards);
+            }
+
             instances.forEach((inst) => {
               const match =
-                inst.dataset.previewVariant === v &&
+                inst.dataset.previewVariant === newVariant &&
                 inst.dataset.previewMode === currentMode;
               inst.hidden = !match;
             });
+            // Sincronizar el preview con el estado recién cargado del editor.
+            renderCards(widgetId);
             // Notificar al widget para re-aplicar layouts y Swiper.
             document.dispatchEvent(
               new CustomEvent('visor:cards-changed', {
